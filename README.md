# Spectra — Landing (scaffold)

Stack:
- Vite + React + TypeScript
- TailwindCSS
- Framer Motion

Scripts:

```
npm install
npm run dev
```

Estructura inicial creada: `src/` con secciones y estilos.
